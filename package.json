{
  "name": "branch-change-tabs",
  "displayName": "Branch Change Tabs",
  "description": "Opens modified files when switching git branches (excluding configured branches).",
  "version": "0.0.1",
  "publisher": "your-name",
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "workspaceContains:.git"
  ],
  "extensionDependencies": [
    "vscode.git"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Branch Change Tabs",
      "properties": {
        "branchTabs.excludedBranches": {
          "type": "array",
          "default": [
            "main",
            "master"
          ],
          "description": "Branch names that should not trigger auto-opening files.",
          "items": {
            "type": "string"
          }
        },
        "branchTabs.closeAllBeforeOpen": {
          "type": "boolean",
          "default": true,
          "description": "Close all open editors before opening changed files."
        },
        "branchTabs.pinOpenedTabs": {
          "type": "boolean",
          "default": true,
          "description": "Pin editors opened by the extension (preview=false)."
        },
        "branchTabs.excludeRegexes": {
          "type": "array",
          "default": [],
          "description": "List of regex strings to exclude file paths from opening.",
          "items": {
            "type": "string"
          }
        },
        "branchTabs.maxFilesToOpen": {
          "type": "number",
          "default": 10,
          "description": "Abort opening files when the number of changed files exceeds this limit."
        },
        "branchTabs.closeAllOnExcludedBranch": {
          "type": "boolean",
          "default": true,
          "description": "Close tabs that were opened by the extension when switching to an excluded branch."
        },
        "branchTabs.baseBranch": {
          "type": "string",
          "default": "",
          "description": "Optional base branch/ref to diff against. If empty, uses upstream if set, otherwise main/master when available."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -watch -p .",
    "lint": "echo \"No lint configured\"",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  }
}
